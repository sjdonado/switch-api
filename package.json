{
  "name": "switch-api",
  "version": "1.0.0",
  "description": "Serverless cloud functions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf dist",
    "build": "babel ./src/routes/*.js ./src/*.js --out-dir dist && copyfiles src/package.json dist",
    "deploy": "yarn --cwd dist/src install && yarn --cwd dist/src start"
  },
  "author": "sjdonado",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "copyfiles": "^2.1.0",
    "rimraf": "^2.6.2"
  }
}
